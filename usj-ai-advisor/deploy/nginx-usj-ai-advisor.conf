# HTTP-only fallback until Let's Encrypt files exist.
server {
    listen 80;
    listen [::]:80;
    server_name usj-advisor.pmediaplus.com;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/html;
    }

    include /etc/nginx/snippets/usj-ai-advisor-locations.conf;
}
